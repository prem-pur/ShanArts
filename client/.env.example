# Copy this file to `.env.development` in this folder, then run `npm start` again.
# Web OAuth client ID (same value as GOOGLE_CLIENT_ID in server/.env).
REACT_APP_GOOGLE_CLIENT_ID=
# Local API (must match server PORT, default 5001).
REACT_APP_API_BASE_URL=http://localhost:5001
