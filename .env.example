## Node (server)
# PORT=5000

## ML model server (FastAPI) — localhost only
ML_SERVER_URL=http://127.0.0.1:8000
ML_TIMEOUT_MS=5000

## Python model artifact paths (joblib)
# In this repo, the pipeline artifacts currently live in ./ai-service/
ML_MODEL_PATH=./ai-service/best_xgboost_delay_model.pkl
ML_TARGET_ENCODER_PATH=./ai-service/label_encoder.pkl

## ML server bind
ML_HOST=127.0.0.1
ML_PORT=8000

